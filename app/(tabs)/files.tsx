import { ElectricScreen } from "@/src/components/electric-screen";

export default function FilesPage() {
  return (
    <ElectricScreen
      eyebrow="Electric Curator"
      headline="Browse documents inside tonal layers."
      description="Instead of divider-heavy panels, files are framed through spacing and soft surface shifts so the content stays calm and readable."
      cardTitle="Files Surface"
      cardBody="Level-based containers replace hard lines for cleaner document browsing."
      chipLabel="No-line rule"
      actionLabel="Review Library"
    />
  );
}
